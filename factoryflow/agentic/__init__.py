"""Step 4 — Robot Prompting (Agentic Control). Master doc Part 6b.

Chain: ``command_parser`` (LLM) -> ``tool_schema`` (validate) ->
``task_executor`` (FSM) -> ``robot_tools`` (skills) -> ``demo_logger``.
"""
