"""Throwaway PyBullet stand-in robot used to exercise the teleop chain
end-to-end (Teammate 3 / Sky) before Step 2's real sim binding is wired up as
the `RobotSink`. Not the RL training environments -- see `simbiote.sim_env`
for those.
"""
