"""Cross-team interface schemas and the skills API Step 2 exposes to Steps 3 & 4.

`actions.py` and `trajectory.py` are jointly owned by Teammates 2, 3 and 4
(per §6.2/§6b.2/§5.7 of the spec) — this copy is Teammate 2's reference
implementation, built so Step 2's own code (BC pretraining, demo ingestion)
has something concrete to run against tonight. Treat any change here as a
group conversation before Steps 3/4 build against a different shape.
"""
